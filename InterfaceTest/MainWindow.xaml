<Window x:Class="InterfaceTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DockPanel Example" Height="300" Width="500">
    <Window.Resources>
        <ContextMenu x:Key="SplitMenu" >
            <MenuItem Header="Split horizontally" Click="SplitHorizontally_Click" />
            <MenuItem Header="Split vertically" Click="SplitVertically_Click" />
        </ContextMenu>
        <DockPanel x:Key="Border" x:Shared="False" ContextMenu="{StaticResource SplitMenu}">
            <TabControl>
                <TabItem Header="Test"></TabItem>
            </TabControl>
        </DockPanel>
    </Window.Resources>
    <Grid x:Name="LayoutRoot" Background="White" ShowGridLines="True">
        <ContentControl Content="{StaticResource Border}" Grid.Row="0"/>
    </Grid>
</Window>

